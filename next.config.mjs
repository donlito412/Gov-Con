/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/Gov-Con',
  assetPrefix: '/Gov-Con',
};

export default nextConfig;
